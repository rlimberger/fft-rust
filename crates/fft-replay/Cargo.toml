[package]
name = "fft-replay"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
fft-book = { path = "../fft-book" }
fft-core = { path = "../fft-core" }
fft-log = { path = "../fft-log" }
fft-profile = { path = "../fft-profile" }

[dev-dependencies]
tempfile = "3"
