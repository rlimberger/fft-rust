[package]
name = "fft-profile"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
fft-core = { path = "../fft-core" }
jiff = "0.2"

[dev-dependencies]
proptest = "1"
